# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/

# --- 虛擬環境 ---
.venv/
venv/
env/

# --- 測試與型別檢查快取 ---
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# --- 本機資料庫 ---
# carbon.db 由 check_schema.py 產生，刪掉再跑一次即可重建，不需版控
carbon.db
*.sqlite
*.sqlite3
*.db-journal

# --- 環境變數（CARBON_DB_URL 等連線設定）---
.env
.env.*

# --- Excel 開啟中產生的鎖定檔 ---
~$*.xlsx
~$*.xls

# --- 編輯器與作業系統 ---
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# --- 官方表單原檔 ---
# 916KB，且內嵌原始製表者的內部路徑。版控裡放的是從它抽出來的
# 代碼表 CSV（data/codes/），註明來源與版本日期，小且可 diff。
溫室氣體排放量清冊表單*.ods
溫室氣體排放量清冊表單*.xlsx

# --- 個人資料，絕不進 public repo ---
# 載具消費明細是完整的個人消費紀錄（買什麼、在哪買、幾點買）。
# 測試資料一律用自編假資料，不要用真的匯出檔。
發票明細*.csv
消費明細*.csv
載具*.csv
data/private/

# --- 內部規劃文件 ---
# 含風險清單、團隊分工評估與「砍功能順序」，是給團隊看的，不是給評審或路人看的。
# 若確定要一併公開，刪掉下面這一行再 git add 即可。
專題執行計畫_v2.xlsx
