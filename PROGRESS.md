# 進度與決策紀錄

換一台電腦接著做時，先讀這一份。程式怎麼裝、怎麼跑在 [README.md](README.md)。

最後更新：2026-08-12（種子資料已進資料庫，62 個測試全綠）

---

## 換電腦怎麼開始

```bash
git clone https://github.com/RyanWu9863/sme-carbon-inventory-tool.git
cd sme-carbon-inventory-tool
python -m venv .venv && .venv\Scripts\activate (PowerShell: Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process; .venv\Scripts\Activate.ps1)
pip install -r requirements.txt
python -m pytest tests/ -q          # 應該看到 62 passed
python scripts/import_seed.py       # 建 carbon.db 並匯入種子資料
```

**兩個檔案不在版控裡**（USB 或雲端硬碟，不要傳進 repo）：

| 檔案 | 為什麼不在 repo | 少了它會怎樣 |
|---|---|---|
| `專題執行計畫_v2.xlsx` | 含風險清單與砍功能順序，是內部判斷，repo 是 public | 沒差，這份文件的「路線圖」一節已經取代它 |
| `溫室氣體排放量清冊表單(範例).ods` | 916 KB 官方原檔，且內嵌原始製表者的內部路徑 | **沒差了**，代碼表已抽成 CSV 進版控 |

**兩個都不再擋路。** 上一版寫「.ods 少了下一步做不了」，那件事已經做完 —— 代碼表
7,603 筆在 `data/codes/`，62 個測試在沒有 .ods 的機器上照樣全綠。.ods 只有官方
改版、要重抽代碼表時才需要。

---

## 專案定位

**自主練習作品，沒有指導老師、沒有評分。**（2026-08-12 確認）

這件事改變的東西寫在下面「路線圖調整」。**沒有改變的是揭露紀律**：v5 試算表裡那些「口試會問」的限制說明照樣要寫。沒有人來問，不代表數字可以不交代來源——那正是這個練習在練的東西。

---

## 現在做到哪

**資料庫現在是滿的了。** 係數、熱值、電力、GWP、7,603 筆代碼表都在 `carbon.db` 裡，而且從資料庫的值重算 12 個係數與試算表逐位相同。還缺的是把資料庫接上計算引擎的服務層。

| 元件 | 狀態 |
|---|---|
| `app/models.py` — 13 張表，對齊官方表一~表七、附表一/二/五~七 | ✅ |
| `app/calculator.py` — 熱值換算、GWP 加總、跨期分攤、完整性檢查 | ✅ |
| `app/seed.py` — 從 v5 試算表讀係數／熱值／電力／GWP／公告出處 | ✅ |
| `scripts/extract_codes.py` — 官方 .ods → `data/codes/*.csv` | ✅ |
| `scripts/import_seed.py` — 代碼表＋係數 → `carbon.db`，可重複執行 | ✅ |
| `tests/` — 62 個測試，全綠 | ✅ |
| 代碼表全量抽出（**7,603** 筆，非 7,639，見下） | ✅ |
| 種子資料寫進資料庫 | ✅ |
| 服務層（資料庫 ↔ 計算引擎） | ❌ **下一步** |
| API | ❌ |
| 使用者介面 | ❌ |

驗收基準：**7.531736 tCO2e**。目前是純函式算出這個數字（`test_totals_match_spreadsheet_table8`）；服務層做完後，要能從資料庫算出同一個數字。

中間有一段已經打通了：`test_factors_survive_the_round_trip` 從資料庫讀係數與熱值、
重算 12 個每單位係數，與試算表 U 欄一致（最大差 4.4e-16）。剩下的是活動數據那一半。

---

## 這次（2026-08-11 ~ 12）做了什麼

### 上傳 GitHub

<https://github.com/RyanWu9863/sme-carbon-inventory-tool>（public）

上傳前檢查過 v5 試算表：裡面是示範資料不是真實單據（「示範小吃店」、電號 `01-23-4567-89`、車牌 `3888-AB` 都是假的，統編與地址是「待填」），程式端沒有金鑰，`db.py` 走 `CARBON_DB_URL` 環境變數。

`tests/` 資料夾原本叫 `test/`，README 寫的卻是 `tests/`，照 README 打指令會失敗。已改成 `tests/`。

### 修正 v5 的一處自相矛盾

「資料來源與限制」第 5 條原本寫「本表燃料類一律採 30」，與第 0 條、GWP表分頁、程式、測試四處採用的 28 衝突——是平台公告修正前的殘留文字。已改寫。

**後來下載的官方表單正好背書**：2026/6/1 修正版的「表五 修正說明」明載，依平台公告，燃料燃燒（含固定及移動設備）採「甲烷」GWP=28。報告裡可以直接引這一條，不必說「我猜 28」。

改法是只替換 `xl/sharedStrings.xml` 裡那一條字串，其餘 zip entry 原樣複製——用 openpyxl 重寫整個工作簿會丟掉公式的快取值，GitHub 預覽會變成一片空白。

### 種子資料從試算表讀，不手抄

`app/seed.py` + `tests/test_seed.py`（新增 11 個測試）。

**為什麼不手抄成 Python 常數**：手抄會產生第二份真相。係數改版時要記得改兩個地方，漏掉一個，程式與試算表就開始各說各話，而且兩邊都「正常執行」，只是答案不同。

附帶好處是這張比對表——試算表 U 欄是 Excel 公式算的，左欄是 Python 算的，兩條路徑獨立：

```
係數編號                        程式重算            試算表        差
TW-F-GASOLINE-S         2.189542995648    2.189542995648   0.0e+00
TW-F-DIESEL-S           2.685856261540    2.685856261540   4.4e-16
TW-F-NG-S               1.906017804484    1.906017804484   2.2e-16
TW-M-GASOLINE-OXI       2.270679160320    2.270679160320   0.0e+00
...（共 12 個，最大差 4.4e-16）
```

原本 `test_calculator.py` 只手抄 5 個燃料進測試檔，現在 12 個全涵蓋，而且在試算表新增燃料時會自動跟著測。

三個刻意的設計：

1. **欄位搬動擋在門口**（`_check_header`）。讀錯欄位的種子資料看起來完全正常——熱值欄讀成 CO2 欄會得到一組合理但全錯的係數，沒有任何錯誤訊息。有測試故意對調欄位確認擋得住。
2. **114 年度電力係數尚未公告，不讓它靜靜消失。** 跳過該列，但年度記在 `electricity_pending_years`。否則盤查 114 年度只會得到「查無係數」，沒人知道是因為政府還沒公告。
3. **試算表沒算過公式時明確報錯。** `data_only=True` 讀的是 Excel 存檔時的快取值；檔案沒被 Excel 開過的話 U 欄會是 `None`，此時直接告知「用 Excel 開啟後存檔再匯入」。

### 官方清冊表單已下載並驗證

`溫室氣體排放量清冊表單(範例).ods`，2026/6/1 修正版。

- 附表五 製程 **1,024** 筆、附表六 設備 **359** 筆、附表七 原燃物料 **6,256** 筆，與計畫表數字完全一致
- 結構是 `序號 | 代碼 | 名稱`，可直接匯入
- 另一個有用的修正說明：表三的外購台電電力代碼更新為 `GG3500`——v5 用的就是 GG3500

### 代碼表抽成 CSV，.ods 從此不必再帶著跑

`scripts/extract_codes.py` → `data/codes/*.csv` + `tests/test_codes.py`（新增 16 個測試）。

**三個發現，跟計畫表記的數字都不一樣：**

1. **筆數少 36 筆。** 原本記的 1,024 / 359 / 6,256 是**含表頭的列數**，實際資料列是
   1,023 / 358 / 6,255。照 7,639 寫驗收測試會直接失敗。
2. **附表七有 33 筆重複。** 官方把序號 6179~6211 那一段（廢液與污染土壤，
   `R-25xx`／`S-01xx`／`S-02xx`）原封不動複製成 6223~6255。`MaterialCode.code`
   是主鍵，不處理會直接擋住匯入。逐筆比對過，33 組全是同代碼同名稱，零衝突。
3. **v5 手挑的 23 筆代碼與官方全量逐字一致**（含 `GG3500`）。代表現有的
   `material_code` 外鍵不會斷，不需要資料遷移。已寫成測試釘住。

去重後 **1,023 + 358 + 6,222 = 7,603 筆**。這才是匯入的驗收基準。

**三個刻意的設計：**

1. **不裝 odfpy／pandas，自己解 XML。** 這支腳本一輩子只跑幾次，卻會讓每個 clone
   的人都多裝一個套件。ODS 就是 zip 裝著 `content.xml`，標準函式庫夠用。

2. **ODS 的儲存格壓縮是會安靜出錯的地方。** ODS 把連續相同的儲存格壓成一個，用
   `number-columns-repeated` 記次數，一列尾端常是「一個空格重複 16384 次」。照實
   展開會炸記憶體；無視 repeat 則會**讓欄位左移，讀出一份看起來完全正常的錯資料**。
   作法是照實展開但取滿三欄就停。

3. **序號連續性檢查，用官方序號當校驗碼。** 這是整支腳本最重要的一道。解析錯誤不會
   拋例外，6,255 讀成 6,250 你不會發現。官方序號本來就是 1..N 連續，驗證它，就不必
   事先知道正確筆數也能證明沒讀漏 —— 一道同時擋掉漏讀、重複讀、順序錯亂。

去重規則是**同代碼同名稱才刪，同代碼不同名稱一律拋錯**。後者現在一筆都沒有，但真
出現時代表官方改了名稱或代碼被重用，該由人判斷；程式替你挑一個，等於把一個需要決策
的問題變成一個看不見的問題。被移除的 33 筆連同它跟哪一筆重複，都記在
`data/codes/README.md`。

測試刻意設計成**不需要 .ods**：去重邏輯用自編假資料測，其餘測已進版控的 CSV。所以
任何一台 clone 這個 repo 的電腦都跑得起來 —— 這正是把代碼表抽成 CSV 的目的。

### 種子資料寫進資料庫

`scripts/import_seed.py` + `tests/test_import_seed.py`（新增 18 個測試，共 62 個）。

匯入結果：代碼表 1,023／358／6,222，GWP 4，公告版本 1，燃料係數 12，熱值 6，電力 2。

**兩個驗收條件，都寫成測試了：**

1. **跑兩次結果相同**（`test_running_twice_changes_nothing`）。不只驗筆數 —— 筆數
   相同也可能是「刪掉再新增」。第二次跑的 tally 必須全部落在 unchanged。
2. **從資料庫重算 12 個係數，與試算表 U 欄一致**（`test_factors_survive_the_round_trip`，
   最大差 4.4e-16）。匯入時欄位對錯（CO2 寫進 CH4 欄）不會有任何錯誤訊息，資料庫
   看起來完全正常，只是每個數字都錯。這是唯一擋得住的東西。實際做過突變測試確認
   它會紅。

**幾個決定：**

- **upsert 而不是「刪掉重建」。** 刪掉重建會讓已算好的 `EmissionResult` 外鍵指向
  消失的列，而且每跑一次 id 就換一輪 —— 快照就不再是快照。
- **公告出處從試算表讀，不手抄。** 「資料來源與限制」B4 寫著「環境部 113年2月5日
  環部授氣字第1139101231號公告」，`FactorSet.version` 直接用公告文號。文號本來就是
  那一版的唯一識別，改版自然變成新的一筆，符合 models.py 的「改版＝新增一筆，永不
  UPDATE 舊的」。為此在 `seed.py` 加了 `FactorSource`。
- **`org_id` 有值的熱值絕不覆蓋。** 那是事業自填的資料，models.py 明講熱值是唯一
  「使用者可以合法覆寫官方值」的東西。有測試守著。
- **整批同一個 transaction，任一步失敗全部回滾。** 匯入到一半的資料庫比空的危險，
  因為它看起來是能用的。

---

## 這次確認的四件事

### 1. 用自己的購買發票替代真實單據 — 可行，但看清楚它替代掉什麼

加油發票只蓋到範疇一的移動燃燒。在 v5 的示範案例裡，**外購電力佔 6.73 of 7.53 tCO2e，也就是 89%**，而且電費單撐著技術上最漂亮的一段：雙月期帳單跨年度、依 45/62 天分攤、分攤後標記推估。

**可行的組合**：盤查對象改成自家／租屋處，電費單 + 加油發票 + 瓦斯帳單一次到位，全部是真的。

**要誠實寫進報告的**：家戶不是「事業」，官方表一的管制編號、統一編號、行業別代碼填不出來。定位改寫成「以家戶作為方法驗證對象，工具本身仍對齊事業清冊格式」。講清楚就沒事，含糊帶過才有問題。

**不能替代的**：使用者測試。資料來源可以是自己，但操作測試必須是外人——自己測自己一定順，那個數據沒有意義。

### 2. 用自己的載具 — 可行，而且是升級

財政部電子發票整合服務平台可匯出消費明細 CSV，今天就能拿到真實資料，整段 QR code 解碼可以不寫。

**要先驗證**（五分鐘的事，但它決定要不要寫回推邏輯）：載具明細裡的加油發票**有沒有品項與公升數**。很多加油站只開「油料 + 金額」。若只有金額就要走油價回推，那是推估值——v5 活動數據登錄第 4 筆就是這個情境（發票僅 1,500 元，以 30.5 元/公升回推）。

⚠️ **載具消費明細是完整的個人消費紀錄，repo 是 public，絕對不能進去。** `.gitignore` 已擋 `載具*.csv`、`消費明細*.csv`、`發票明細*.csv`、`data/private/`。測試資料一律自編假的。

### 3. 沒有指導老師 — 這一項不再是阻塞項

範疇一二的界定已經自洽：v5 表二寫了三條排除理由（製程、冷媒、化糞池），「資料來源與限制」第 7 條揭露了不含範疇三。沒有人點頭，這個界定一樣站得住。

原計畫表把「跟老師確認範疇界定」列為阻塞 Y，該項作廢。

### 4. 官方表單 — 檔案正確（見上一節）

---

## 路線圖調整（取代計畫表的 W1~W9）

自主練習沒有口試，時間重新分配：**W9 收尾大幅縮減，省下的時間給 W4 電費單解析**，因為後者是作品集裡看得見的東西，簡報不是。

| 順序 | 做什麼 | 原計畫 | 調整後 |
|---|---|---|---|
| 1 | 代碼表抽成 CSV | W2② | ✅ 完成，7,603 筆在 `data/codes/` |
| 1.5 | 種子資料寫進資料庫 | W2① | ✅ 完成，`scripts/import_seed.py` |
| 2 | 服務層：資料庫 ↔ 計算引擎 | W2④ | **現在做**，驗收基準是從 DB 算出 7.531736 |
| 3 | API | W2③ | 服務層做完後只是薄薄一層 |
| 4 | 最小可用介面：輸入到看見表八 | W3 | 核心 |
| 5 | 報表與 Excel 匯出 | W7 | 核心 |
| 6 | 電費單解析 | W4，可砍 | **升級為值得做**（省下的時間給它） |
| 7 | 發票載具 CSV 匯入 | W5，可砍 | 用自己的載具，成本降低 |
| 8 | 使用者測試 | W8，不可砍 | 保留，但找同學／家人即可，不需實際店家 |
| 9 | 書面報告 → 改成 README + demo 影片 | W9 | **大幅縮減**，簡報與演練取消 |
| — | 係數自動對應 | W6，第一個砍 | 維持放棄，用下拉選單人工指定 |

**兩件仍然不要砍**：使用者測試（沒有真人操作過的工具，problem 都藏著）、demo 影片（能獨立播放，不依賴環境）。

### 順序有一個變動，理由要記住

原本規劃「官方代碼表匯入」排最後，因為當時檔案還沒有。**現在要提前到最前面**——`PublishedFactor.material_code` 是指向 `MaterialCode` 的外鍵，代碼表沒先進資料庫，係數就沒有東西可以指。

---

## 下一步（可直接開工）

~~1. 從 .ods 抽出三張代碼表~~　✅ 完成（7,603 筆，非 7,639）
~~2. `scripts/import_seed.py`~~　✅ 完成（可重複執行，係數往返一致）

**服務層：`app/service.py`（暫名）。** 這是整個專案的核心，缺的就是它。

現在的狀態是兩頭都好、中間沒接：`calculator.py` 收的是攤平的 dataclass
（`FuelFactorInput` + 熱值 + GWP dict），資料庫裡放的是 ORM 物件。服務層要做的事：

1. **查係數**：給 `EmissionSource.factor_key` 與盤查年度，取出 `PublishedFactor`、
   對應的 `HeatingValue`（事業自填優先於系統預設）、`GwpValue`，組成
   `FuelFactorInput` 餵進 `derive_fuel_factor`。
2. **算一筆活動數據**：`ActivityRecord` → 跨期分攤（`allocate_period`）→
   `calculate_fuel` 或 `calculate_electricity` → 寫回 `EmissionResult`，**連同快照
   欄位**（`heating_value_used`、`factor_set_version`、`ch4_gwp_used`、`calc_trace`）。
3. **算整個年度**：彙總成表八，並跑 `check_completeness`。

**驗收基準：把 v5「活動數據登錄」那幾筆餵進去，從資料庫算出 7.531736 tCO2e。**
目前這個數字只有純函式算得出來（`test_totals_match_spreadsheet_table8`）。

兩件要注意的：

- **推估要強制填理由。** `ActivityRecord.estimation_basis` 在 models.py 註明「推估時
  必填，由服務層強制」—— 資料庫沒有這個約束，是服務層的責任。跨期分攤後
  `Allocation.quality_hint` 會回「推估」，那時就要擋。
- **電力不可再乘 GWP。** 走 `calculate_electricity` 而非 `calculate_fuel`，且要依
  盤查年度取係數。114 年度尚未公告，此時要給得出「政府還沒公告」而不是「查無資料」。

---

## 待決事項

- **熱值假設**：天然氣目前用 NG1(消費面) 8,107 kcal/m³。熱值表四個天然氣數值最大差 48%，實務上應改用瓦斯公司費率公告的當期熱值。這是最大的未決假設，v5「資料來源與限制」第 3 條已揭露。
- ~~**`HeatingValue.factor_key` 的鍵怎麼定**~~ → **已解決。** 依 `material_code` 去重，
  12 個燃料列收斂成 6 筆；`factor_key` 也填 `material_code`。實際查過六組全部一致
  （汽油四列都是 7,520 kcal/公升）。一致性檢查照樣寫進 `collapse_heating_values`，
  不一致就報錯 —— 現在沒撞到不代表以後不會。自然鍵是 `(material_code, org_id)`，
  `org_id` 為 None 才是系統預設，事業自填的不會被匯入蓋掉。
- ~~**代碼表對不上小店實況**~~ → **已查證，而且現在的分類是錯的。**
  全量代碼表出來後查過 358 筆設備代碼：官方有 **`B001 燃氣台爐`**，那就是瓦斯爐；
  另有 `B002 燃氣熱水器`。當初查不到是因為只看了 v5 手挑的 9 筆子集，不是官方沒有。

  v5 表三目前把瓦斯爐歸在 `0020 燃氣鍋爐`（工業鍋爐），**應改為 `B001`**。

  **不影響 7.531736。** 設備代碼是表三的描述性欄位，排放量走的是
  `factor_key` → `PublishedFactor`，燃料端仍是 `350008 液化石油氣`。所以這是分類
  正確性的修正，不是數字修正 —— 但正因為它不會讓任何測試變紅，才更要記下來。

  待辦：改 v5 表三的設備代碼，並在「資料來源與限制」補一句。改 xlsx 時注意
  openpyxl 重寫整個工作簿會丟掉公式快取值（見上面「修正 v5 的一處自相矛盾」）。
- **`models.py` 用 `dt.datetime.utcnow`**，Python 3.12 起標記為 deprecated。不急，但遲早要改成 `dt.datetime.now(dt.UTC)`。
